{
  "name": "@pitickets/common",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del-cli ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m\"publish updates\" && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^3.0.0",
    "typescript": "^3.8.3"
  }
}
